import React from 'react';
import { SafeAreaView } from 'react-native';
import { styles } from '../components/Styles'

function Account(props) {
    return (
        <SafeAreaView style={styles.flexContainer}></SafeAreaView>
    );
}

export default Account;